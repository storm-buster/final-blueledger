# Minimal requirements - only essential packages with guaranteed Windows wheels
# Use this if the full requirements.txt fails

# Core web framework
fastapi>=0.100.0
uvicorn[standard]>=0.23.0

# Data validation (use version with pre-built wheels)
pydantic>=2.0.0,<3.0.0
pydantic-settings>=2.0.0

# Data science (these have Windows wheels)
numpy>=1.24.0
pandas>=2.0.0

# ML (optional - can skip if not needed immediately)
# xgboost>=2.0.0
# scikit-learn>=1.3.0

# Supabase
supabase>=2.0.0

# Utilities
python-multipart>=0.0.6
python-dotenv>=1.0.0
requests>=2.31.0

# Image processing (optional)
# opencv-python>=4.8.0
# Pillow>=10.0.0
